site.host=https://lanwen.github.io/frmtr
render.index=false
render.archive=false
render.feed=false
render.tags=false
render.sitemap=false
template.landing.file=index.ftl
template.encoding=UTF-8
output.extension=.html
default.status=published
# Shown in the top-right of the site nav. Placeholder for now; swap for the real
# release version (or have the Gradle build inject it) when publishing.
# Accessed in templates as ${config.frmtr_version} (JBake maps dots to underscores).
frmtr.version=0.0.0-dev
frmtr.repo=https://github.com/lanwen/frmtr
