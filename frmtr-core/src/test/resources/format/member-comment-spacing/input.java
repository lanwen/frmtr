package sample;

final class CommentSpacing {
    private final Failure failure;

    // Keep this note paired with the next line.
    // The second note belongs to the constructor.
    CommentSpacing(Failure failure) {
        this.failure = failure;
    }
}
