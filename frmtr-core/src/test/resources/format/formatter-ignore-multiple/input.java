public class IgnoredFormatterSections {
  int retryBudget;

  public void rebuildSearchIndex(int accountId, CatalogClient catalogClient, int retryLimit, int timeoutSeconds, int batchSize, int pageNumber, int shardId, int workerCount, int priority, int auditLevel) {
    for          (int i = 0; i < accountId; i++)                                     {
      catalogClient.loadCatalog().loadCatalog().loadCatalog().loadCatalog().loadCatalog().loadCatalog().loadCatalog().loadCatalog().loadCatalog().loadCatalog().loadCatalog();
    }
  }

  // frmtr-ignore
  public void rebuildIgnoredSearchIndex(int accountId, CatalogClient catalogClient, int retryLimit, int timeoutSeconds, int batchSize, int pageNumber, int shardId, int workerCount, int priority, int auditLevel) {
    for          (int i = 0; i < accountId; i++)                                     {
      catalogClient.loadCatalog().loadCatalog().loadCatalog().loadCatalog().loadCatalog().loadCatalog().loadCatalog().loadCatalog().loadCatalog().loadCatalog().loadCatalog();
    }
  }
}
