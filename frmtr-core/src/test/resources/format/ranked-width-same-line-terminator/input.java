class LedgerSnapshotStore {
    void terminatorCountsForTheInitializerLine() {
        final ConcurrentHashMap<String, ConcurrentHashMap<Integer, Long>> snapshot = new ConcurrentHashMap<>(map.size());
    }

    String terminatorCountsForTheBinaryReturn() {
        return "ClusterDelta(" + "changedBrokers=" + changedBrokers + ", changedControllers=" + changedControllers + ')';
    }

    public void terminatorCountsForTheThrowsClauseBraceOnSignatureLine() throws ExecutionException, InterruptedException {
        rebuild();
    }
}
